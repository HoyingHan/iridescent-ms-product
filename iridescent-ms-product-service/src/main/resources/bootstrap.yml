spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: qwerasdf
    url: jdbc:mysql://localhost:3306/iridescent?characterEncoding=utf-8&useSSL=false
    jpa:
      show-sql: true
    jackson:
      default-property-inclusion: non_null
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

